<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:MainActivity="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item android:id="@+id/today"
        android:icon="@drawable/today"
        android:title="@string/today"
        MainActivity:showAsAction="always"
        />
    <item android:id="@+id/week"
        android:icon="@drawable/week"
        MainActivity:showAsAction="always"
        android:title="@string/week" />
    <item
        android:id="@+id/search_zip"
        MainActivity:actionViewClass="android.widget.SearchView"
        MainActivity:showAsAction="always"
        android:title="Search Zip"/>
</menu>